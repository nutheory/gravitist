'use strict';

var defaultSyncReturn = 'path/to/package-scripts.js';

var mock = {};

module.exports = {
  sync: jest.fn(function () {
    return mock.hasOwnProperty('syncReturn') ? mock.syncReturn : defaultSyncReturn;
  }),
  mock
};