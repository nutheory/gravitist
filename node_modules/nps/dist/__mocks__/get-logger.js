'use strict';

var mock = {
  error: jest.fn(),
  warn: jest.fn(),
  info: jest.fn()
};

var getLogLevel = jest.fn(function () {
  return 'info';
});

module.exports = function () {
  return mock;
};
Object.assign(module.exports, { mock, getLogLevel, clearAll });

function clearAll() {
  mock.error.mockClear();
  mock.warn.mockClear();
  mock.info.mockClear();
}