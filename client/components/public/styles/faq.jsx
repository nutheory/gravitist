import { StyleSheet, css } from 'aphrodite'
import { ss, c } from '../../../styles/helpers'

const faq = StyleSheet.create({
  container: {
    width: '70%',
    margin: '80px auto',
  },
  card: {
    marginBottom: '1rem',
  }
})

export default faq
