import { StyleSheet, css } from 'aphrodite'
import { ss, c } from './helpers'

const orderNew = StyleSheet.create({

mainContainer: {
  maxWidth: '768px',
  padding: '2rem',
},
container: {
  paddingBottom: '2rem'
},

})

export default orderNew
