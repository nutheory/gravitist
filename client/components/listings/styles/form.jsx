import { StyleSheet } from 'aphrodite'
import { ss, c, b } from '../../../styles/helpers'

const listingForm = StyleSheet.create({
  featuresList: {
    marginTop: '2rem'
  }
})

export default listingForm
